{
  "name": "@git-lfs-hub/docs",
  "type": "module",
  "private": true,
  "packageManager": "bun@1.3.14",
  "scripts": {
    "config:vars": "git-lfs-hub-config vars .",
    "compile": "tsgo --noEmit",
    "lint": "oxlint .",
    "prebuild": "bun run compile && bun run lint",
    "build": "docmd build",
    "dev": "docmd dev"
  },
  "devDependencies": {
    "@docmd/core": "^0.8.6",
    "@git-lfs-hub/config": "file:../config",
    "@types/bun": "^1.3.14",
    "@typescript/native-preview": "^7.0.0-dev.20260605.1",
    "oxlint": "^1.68.0"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "handlebars": "^4.7.9"
  }
}
